<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Albums</title>
  </head>
  <body>

    <div class="container">

        <div class="row">
            <div class="col-4-sm">
                <h1>Welcome to PhotoAlbums</h1>
              <% @albums.each do |album| %>

                <%= link_to album.name, album_path(album) %> <br>
              <% end %>
              <%= link_to "create album", new_album_path, :class => 'btn btn-outline-success' %>
              <%= link_to "add photos", new_photo_path, :class => 'btn btn-outline-warning' %>
            </div>
        </div>


    </div>


  </body>
</html>
