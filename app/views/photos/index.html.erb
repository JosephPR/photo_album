<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>

    <h1>All Photo's</h1>
    <div class="pic-grid">

      <div class="container">
        <div class="row">
          <div class="col-sm">

            <% @photos.each do |photo| %>
              <div class="card" style="width: 18rem;">
                <%= image_tag photo.image_url, :class => 'card-img-top' %>
                <div class="card-body">
                  <h5 class="card-title">
                    <h3><%= link_to photo.name, photo_path(photo) %></h3>
                  </h5>
                </div>
              </div>
            </div>
            <% end %>
          </div>
        </div>

    </div>
  </body>
</html>
