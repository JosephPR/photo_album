<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>New Photo</title>
  </head>
  <body>

    <div class="container">
      <div class="row">
        <div class="col-sm">
          <%= bootstrap_form_for @photo do |f| %>

          <h3>  <%= f.label :Name %></h3>
            <%= f.text_field :name %>
            <h3><%= f.label :Image %></h3>
            <%= f.text_field :image_url %>
            <br><br>
            <%= f.collection_select :album_id, @albums, :id, :name %>
            <br><br>
            <%= f.submit  "Add Photo", :class => 'btn btn-outline-primary' %>
          <% end %>
        </div>
      </div>
    </div>

  </body>
</html>
