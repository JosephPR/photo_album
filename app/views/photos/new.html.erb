<%= form_for @photo do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :image_url %>
  <%= f.text_field :image_url %>
  <%= f.collection_select :album_id, @albums, :id, :name %>
  <%= f.submit "Add Photo" %>
<% end %>
