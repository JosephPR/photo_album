<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>New Album</title>
  </head>
  <body>

    <div class="container">
      <div class="row">
        <div class="col-sm">
            <%= bootstrap_form_for @album do |f| %>

            <%= f.text_field :name %>
            <%= f.text_field :album_url %>
            <%= f.text_field :description %>
            <%= f.submit "Create", :class => 'btn btn-outline-primary' %>
            <% end %>
        </div>
      </div>
    </div>

  </body>
</html>
