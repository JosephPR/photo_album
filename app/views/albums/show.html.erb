<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
        <div class="container-fluid">
          <h1><%=link_to @album.name, albums_path %></h1>
          <br>

          <% @photos.each do |photo| %>
          <div class="container">
            <div class="row">
              <div class="col-sm photo">
                <%= photo.name %> <br>
            <div>
               <%= image_tag photo.image_url, :class => "img-fluid"%>
               <%= link_to "Delete Photo", photo_path(photo), method: "DELETE", :class => 'btn btn-outline-light', :style => 'color: rgb(107, 35, 25)', data: {confirm: "Are you sure?"}  %>          <hr>

            </div>
                <% end %>
              </div>
              <%= link_to 'Edit Album', edit_album_path(@album.id), :class => "btn btn-outline-light", :style => 'color: rgb(107, 35, 25)' %>
              <br>
              <%= link_to "Delete Album", album_path(@album.id), method: "DELETE", :class => 'btn btn-outline-light', :style => 'color: rgb(107, 35, 25)', data: {confirm: "Are you sure?"}  %>
            </div>
          </div>

        </div>

  </body>
</html>
