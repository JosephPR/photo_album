<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
        <div class="container-fluid">
          <h1><%=link_to @album.name, albums_path %></h1>
          <br>

          <% @photos.each do |photo| %>
          <div class="container">
            <div class="row">
              <div class="col-sm photo">
                <%= photo.name %> <br>
            <div>
               <%= image_tag photo.image_url, :class => "img-fluid"%>
            </div>
                <% end %>
              </div>
            </div>
          </div>

        </div>

  </body>
</html>
